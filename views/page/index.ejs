<!DOCTYPE html>
<html>

<head>
  <%- include("../inc/head", { page: "Home", data: data, meta: meta }); %>
</head>

<body class="container">

  <header>
    <%- include("../inc/nav", { page: "Home", data: data, meta: meta }); %>
  </header>

  <main>
    <div class="jumbotron">
      <p>
        Welcome to ヴィツ.js,<% if (data) { %> <em><%= data.name %></em>,<% } %> here you can practice your ひらがな, カタカナ, and 漢字.  There are multiple styles of flashcard game to be had here, please peruse the game list <% if (!data) { %> once logged in<% } %>.  Top scores are tracked in the database so you can always relive your high scores!
        <br>
      </p>
      <% if (!data) { %>
      <p>
        If you want to continue, please sign in using one of the methods below.
        <br>
      </p>
      <button type="button" class="btn btn-outline-info" onclick="window.location.href='/login/google';">
        <i class="fab fa-google"></i> &nbsp;Sign in with Google
      </button>
      <button type="button" class="btn btn-outline-info" onclick="window.location.href='/login/facebook';">
        <i class="fab fa-facebook-f"></i> &nbsp;Sign in with Facebook
      </button>
      <% } %>
    </div>
  </main>

  <footer>
    <%- include("../inc/foot", { page: "Home", data: data, meta: meta }); %>
  </footer>

</body>

</html>